<p id="notice"><%= notice %></p>

<h1>Landing Page</h1>

<p>Total jobs: <%= @jobs.size %></p>

<%= form_with(model: Job.new, local: true) do |form| %>
  <div class="actions">
    <%= form.submit 'Launch a Job' %>
  </div>
<% end %>
